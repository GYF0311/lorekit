# LEGACY.md — 待重构清单

> 项目计划全面重构，所以本文是**重构 punch list**（按优先级排序），不是"暂不动"清单。
> 每条注明：何处违反 / 哪条规范触发 / 建议处理顺序。
>
> 不在本文的：
>
> - **F6 中文目录名硬编码**（`'原料'` / `'知识库'` / `'_工作台'` 等）—— schema 设计决定，不是技术债，CONVENTIONS Do Not #8 已封死
> - **bilingual 注释**——CONVENTIONS 已规定中文为主，新代码自动遵守，老代码增量改时一起改即可

---

## P0 — 拦路（红线已触发，挡住后续工作）

| #    | 项目                   | 现状                                                                           | 触发            | 建议                                                                                                                                                 |
| ---- | ---------------------- | ------------------------------------------------------------------------------ | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0-1 | `lib/vectordb.ts` 拆分 | 1115 行（schema + 3 种 query + chunker glue + status + parse helpers + RRF）   | Do Not #9 / #12 | 按职责拆：`vectordb/schema.ts` / `query-flat.ts` / `query-layered.ts` / `query-bm25.ts` / `query-hybrid.ts` / `build-layered-index.ts` / `status.ts` |
| P0-2 | `lib/fetcher.ts` 拆分  | 848 行（generic + weixin + gist + github 四套抓取，frontmatter 拼装重复 4 遍） | Do Not #12      | 按 route 拆：`fetcher/web.ts` / `weixin.ts` / `gist.ts` / `github.ts` + 共用 `frontmatter.ts` / `images.ts`                                          |

---

## P1 — 新规范配套基础设施（CONVENTIONS 落地的前置依赖）

| #       | 项目                                                                 | 现状                                                                                                                                                                                                                        | 触发                                                                            | 建议                                                                                                                                                                                                                       |
| ------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1-1    | 建 `src/lib/paths.ts`，统一 exclude 列表                             | 4-5 套独立维护：`corpus.ts EXCLUDE_NAMES` / `vectordb.ts EXCLUDE_PREFIXES,INCLUDE_DIRS,EXCLUDE_NAMES` / `commands/index.ts INDEX_EXCLUDE_DIR_PREFIXES` / `lint.ts SKIP_*_PREFIXES,SKIP_*_BASENAMES` / `snapshot.ts EXCLUDE` | 加新顶层目录易漏改某处                                                          | 一份 `paths.ts` 导出 named 集合（`vectorIncludeDirs` / `vectorExcludePrefixes` / `lintSkipFrontmatter` 等），各处 import                                                                                                   |
| P1-2    | 建 `src/lib/date.ts`，统一日期 helper                                | `today()` 在 ingest.ts；`todayYMD/tsToYMD/normalizeDateText` 在 fetcher.ts；`pad` 在 init / index / audit / snapshot / ingest 各自重写                                                                                      | 重复代码                                                                        | 一份 `date.ts` 导出 `todayYMD()` / `padDate()` / `normalizeDateText()`                                                                                                                                                     |
| P1-3    | `logger.ts` 加等级 + 修 bad 走 stderr                                | 当前只 `ok` / `bad` / `warn` / `err`，缺 `info` / `debug`；且 `bad` 错误地写 stdout                                                                                                                                         | CONVENTIONS #2/#3 要求"统一走 logger"+"人类信息走 stderr"，老代码迁移时常缺等级 | 加 `info(msg)` / `debug(msg)`；`debug` 受 `LOREKIT_DEBUG=1` 环境变量控制。**同时把 `bad` 从 stdout 改到 stderr（当前行为是 bug，CONVENTIONS #3 规定了但 logger 自身没落地）。**                                            |
| P1-4    | 加 `npm run verify` 与 smoke test                                    | 当前 scripts 只有 `build` / `dev` / `prepublishOnly`                                                                                                                                                                        | CONVENTIONS #11 强制要求                                                        | 用 `node:test`（Node 18+ 内置，零依赖；CONVENTIONS #11 已锁定）。先写 `cli --version` / `lorekit init --minimal /tmp/xxx` / `lorekit doctor` 等 happy path                                                                 |
| P1-5    | 加 lint / format 配置                                                | 无 eslint、prettier、editorconfig                                                                                                                                                                                           | CONVENTIONS #2/#4/#5 等要可机检                                                 | 至少加 eslint + `@typescript-eslint`，规则覆盖 no-console / no-explicit-any / no-empty / ts-expect-error                                                                                                                   |
| P1-6    | `src/cli.ts` `showBanner()` 内 `require()`                           | ESM 文件里 `require('./lib/corpus.js')` / `require('node:fs')` / `require('better-sqlite3')`，且 tsup bundle 后的运行时路径不可靠                                                                                           | Do Not #1 + B1 潜在运行时崩溃                                                   | 改静态 import 或 `await import()`。**从 P2 升上来**：B1 指出现在能跑是巧合，bundle 后 `./lib/corpus.js` 的相对路径可能解析不到，属于"基础设施级"风险，跟其他 P1 一起处理                                                   |
| ✅ P1-7 | ~~`src/commands/index.ts:77` `fm.updated instanceof Date` 类型错误~~ | `Frontmatter.updated` 类型声明 `string`，gray-matter 实际可能返回 `Date`                                                                                                                                                    | 阻塞 `npm run verify`                                                           | **完成于 2026-04-19 批次 0**：`lib/corpus.ts` `Frontmatter.updated` → `string \| Date`。`created` 暂未改（无 `instanceof Date` 调用，类型不报错；将来若需要再补）                                                          |
| ✅ P1-8 | ~~`src/commands/init.ts:101` 绝对路径处理 bug~~                      | `path.join(cwd, abspath)` 拼错位                                                                                                                                                                                            | 用户传绝对路径 init 默默走错位置                                                | **完成于 2026-04-19 批次 0**：`init.ts` `join(process.cwd(), targetPath)` → `resolve(targetPath)`。验证：`lorekit init /tmp/xxx` 现在正确在 `/tmp/xxx` 创建 corpus。**待补**：加一条 smoke 用绝对路径 init（留给后续批次） |

---

## P2 — CONVENTIONS 违反点（逐个修，配合 P1 工具就位后批量做）

| #    | 文件 / 位置                                                                                                                                                                       | 违反                                                                              |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| P2-1 | `src/commands/vector.ts` query action 里 `await import('node:fs')` / `await import('node:path')`                                                                                  | CONVENTIONS #8（标准库不该动态 import）                                           |
| P2-2 | 全仓 `catch { /* ignore */ }` / `catch {}`：cli.ts × 2，fetcher.ts ≥ 5 处，vectordb.ts ≥ 3 处，stats.ts × 2，corpus.ts × 1，root-index.ts × 1                                     | Do Not #3（沉默 catch）                                                           |
| P2-3 | `src/lib/vectordb.ts` 多处 `as any` / `Db = any`；`src/commands/ingest.ts` `patch.status = opts.status as any`；`src/lib/fetcher.ts` `// @ts-ignore`                              | Do Not #4（裸 as any / @ts-ignore）                                               |
| P2-4 | 直接 `console.log` / `console.error` / `chalk.xxx()`：init.ts、snapshot.ts、restore.ts、audit.ts、stats.ts、vector.ts、search.ts、ingest.ts、index.ts、doctor.ts、sync.ts、cli.ts | Do Not #2（统一走 logger）                                                        |

> **批次 14 遗留备注（2026-04-19）**：批次 13/14 完成主线 sweep 后剩余：
>
> - `src/commands/install-skills.ts` 3 处 → **批次 14b 补丁处理**（见 WORKLOG 2026-04-19 批次 14b）
> - `src/commands/fetch.ts` 5 处 → **留给批次 21 拆 fetcher 时一并做**（避免现在改完拆库时又动）
> - `src/lib/vectordb.ts` 7 处 → **留给批次 22 拆 vectordb 时一并做**（原计划）
| P2-5 | `src/lib/vectordb.ts` 顶部 `eslint-disable-next-line @typescript-eslint/no-explicit-any`                                                                                          | 仓库目前没有 ESLint 配置，注释指向不存在的规则；P1-5 落地后可以激活，否则该行删掉 |
| P2-6 | `process.exit(1)` vs `process.exit(2)` vs `process.exitCode = 1` 混用                                                                                                             | CONVENTIONS #4（统一退出码语义）。整改时按"参数错→2，运行时→1"重新分类            |

> 原 P2-1（cli.ts 的 ESM `require()`）已升级到 P1-6，理由见该项 + P4-1（B1）。

---

## P3 — 杂项 / 文档同步

| #    | 项目                                                   | 现状                                                                                                                                                                                                                               | 建议                                                                                                                                                    |
| ---- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P3-1 | `README.md` 版本号                                     | 写 v0.2.0，实际 `package.json` 0.3.0                                                                                                                                                                                               | sync 到 `VERSION` 文件值                                                                                                                                |
| P3-2 | `integrations/claude-code/install.sh` & `uninstall.sh` | 疑似已被 `lorekit install-skills --target claude-code` 取代                                                                                                                                                                        | 确认后删；如还需 shell 入口，至少在脚本里 `exec lorekit install-skills` 转发                                                                            |
| P3-3 | 仓库根 `.wiki/` 目录                                   | 可能是开发测试漏 commit 的 corpus 元数据                                                                                                                                                                                           | 确认后加进 `.gitignore` 的根级排除                                                                                                                      |
| P3-4 | `src/commands/index.ts` 文件名歧义                     | 名字像 barrel export，实际是 `lorekit index` 命令                                                                                                                                                                                  | 拆 vectordb 时一起 rename 为 `commands/build-index.ts` 或 `commands/dir-index.ts`                                                                       |
| P3-5 | `src/commands/fetch.ts` & `vector.ts` 未做 NaN 守卫    | `parseFloat(opts.threshold)` / `parseInt(opts.topK, 10)` 直接用                                                                                                                                                                    | 加 NaN / 范围校验，失败按退出码 2 处理                                                                                                                  |
| P3-6 | `dist/` 提交进 git 导致 commit 噪声                    | 每次源码改动 dist 也变                                                                                                                                                                                                             | 维持现策略（README 承诺免构建可用），但可在 CI 加"`dist/` 必须与源码同步"校验                                                                           |
| P3-7 | `dist/cli.js` 提交纪律                                 | 2026-04-19 session 开始时 `dist/cli.js` 处于 staged 状态，期间已被纳入 commit，当前 `git status` 已 clean。**一次观察**：session 开始时 dist/cli.js 处于 staged 状态，可能是 src 改动后 build 产出未及时 commit 所致（未深入确认） | **纪律性约束**（不需要"修一次"）：今后任何 `src/` 改动必须在同一 commit / PR 内同步 `npm run build` 产出的 `dist/`。P3-6 的 CI 校验落地后此项可以自动化 |

---

## P4 — 扫描发现的潜在问题（待验证）

> 这些是 Step 1 扫描时观察到、**未实际复现**的可疑点。处理前先验证现象是否真实存在。
> （原 B1 cli.ts ESM `require()` 已升级到 P1-6，不在本节。）

| #         | 位置                                                                                                                                                      | 现象                                                                                     | 严重度             | 处理阶段                                                                                                                                                                                                  |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P4-1 (B2) | `src/commands/ingest.ts:144` `--wiki-page <path...>` variadic + `--step archive,wiki` 命令链                                                              | 多次追加 wiki-page 时是否真的去重、列表前置项目录是否保留，未实测                        | 低                 | **待验证**：写一个 record 链式调用的 smoke test 当场暴露                                                                                                                                                  |
| P4-2 (B3) | `src/commands/snapshot.ts:67` `manifest.json` 写到 `<corpus>/.wiki/snapshots/` 后 `unlinkSync`                                                            | 期间若 `tar.create` 抛错则 manifest 残留；且 manifest 在被打包目录内，理论上可能包进自己 | 低                 | **P3 杂项**：改用 `os.tmpdir()` 写 manifest，或包完后再 unlink + try/finally                                                                                                                              |
| P4-3 (B4) | `src/commands/restore.ts:158` `rmSync(tmpDir, { recursive: true, force: true })` 清理临时解压目录                                                         | 这是 `rm -rf` 等价。虽然路径锁定在 `os.tmpdir()`，但触发先生 CLAUDE.md 全局规则的精神    | 中（数据安全相关） | **保留 `rmSync`**，但必须加注释明确：`// 仅限 os.tmpdir() 子目录，不许扩展到任何用户数据路径`。重构时连同注释一起落地，路径必须由 `tmpdir() + 'lorekit-restore-' + Date.now()` 构造，禁止接受外部路径参数 |
| P4-4 (B5) | `src/lib/fetcher.ts:259` 微信图片 `data-w` / `data-ratio` / `data-type` / `data-s` / `srcset` 被 `removeAttr`，但 `<picture>` 与 `<source srcset>` 没处理 | 部分使用 `<picture>` 的微信文章可能图片丢失                                              | 低                 | **在 P0-2 拆 fetcher 时顺手做**：拆出 `weixin.ts` 后单独补 picture/source 处理 + 一条 fixture 测试                                                                                                        |
| P4-5 (B6) | `src/commands/vector.ts:47` `filePath.replace(corpus + '/', '')`                                                                                          | 字符串替换，若 corpus 路径在 filePath 里出现两次（罕见）会替换错位                       | 低                 | **P3 杂项**：改用 `path.relative(corpus, filePath)`                                                                                                                                                       |
| P4-6      | `src/lib/fetcher.ts` 不抽 `source_date`（发布日期）                                                                                                        | 昨晚真实 ingest `claude.com/blog/...` 验证发现：原文页面多处可见 "April 15, 2026"，但 fetcher 产物 frontmatter 缺 `source_date` 字段。所有 claude.com / Webflow 类站点受影响 | 低                 | **留给批次 21 拆 fetcher 时一并修**：在 `web.ts` parseGeneric 阶段识别常见日期 pattern（`<time datetime>` / meta[property=article:published_time] / JSON-LD）回填 frontmatter.source_date                      |
| P4-7      | `src/lib/fetcher.ts` 不生成 `slug` 字段                                                                                                                   | 昨晚真实 ingest 验证发现：lint 报 `missing frontmatter field: slug`。frontmatter-spec 把 slug 列为必填，所有 fetch 产物都触发这条                                     | 低                 | **留给批次 21 拆 fetcher 时一并修**：在共用 `frontmatter.ts` 里按 title / URL 最后一段生成合法 slug（kebab-case，ASCII fallback）                                                                           |
| P4-8      | Windows 兼容性 — 路径分隔符硬编码                                                                                                                         | `src/lib/vectordb.ts:182 / 185 / 837` + `src/lib/paths.ts:98` 共 4 处用 `rel.startsWith(prefix + '/')` 判断目录归属。Windows 上 Node `path.relative()` 返回反斜杠 `\`，这些判断永远 false → 该排除的目录没排除（向量化、索引、lint 全部受影响） | 低                 | **暂不动**：当前无 Windows 用户需求。等真有反馈时单批处理；技术方案是统一改用 `path.sep` 或路径归一化（`rel.split(path.sep).join('/')`）。批次 22 拆 vectordb 时如重写这部分查询逻辑，可顺手做 vectordb 的 3 处；paths.ts 的 1 处独立做 |
