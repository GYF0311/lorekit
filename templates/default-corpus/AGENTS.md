# AGENTS.md

See `CLAUDE.md` for the authoritative ruleset.

This file exists so that non-Claude harnesses (Codex, Cursor, Aider, etc.) can find the same corpus constitution via their conventional entry point. The content of record lives in `CLAUDE.md`.
