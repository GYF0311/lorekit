.DS_Store
*.log
__pycache__/
*.pyc
.venv/
node_modules/

# 临时探针 / 测试残留
tmp/

# corpus 衍生物（不进 git）
.wiki/vector.sqlite
.wiki/vector.sqlite-wal
.wiki/vector.sqlite-shm
.wiki/provider_cache/

# 本仓库根本不该有 .wiki/（那是 corpus 元数据），整体排除避免开发探针漏 commit
/.wiki/

# _INDEX.md 是 sync 自动生成的目录索引，不进 lorekit 工具仓库
# （corpus 在自己的 .git 里管，跟 lorekit 仓库无关）
_INDEX.md

# Claude Code worktrees
.claude/

# CMAP runtime/candidate artifacts (canonical facts live in .context/*.md and modules)
.context/generated/
.context/out/
.context/inbox/
